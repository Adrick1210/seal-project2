<!DOCTYPE html>
<html lang="en">
  <%- include("./partials/head.ejs") %>
  <body>
    <a href="/todos">Return to list</a>
    <div>
        <form action="/todos/<%= todos._id %>?_method=PUT" method="post" class="field">
            <h1>Edit "<%= todos.title %>"</h1>
            <label
              >Title:</label>
              <input 
              type="text" 
              name="title" 
              value="<%= todos.title %>"
              required/>
            
            <br>
            <label
              >Description:</label>
              <textarea
                name="description"
                value="<%= todos.description %>"
                required>
            </textarea>
        
        <br>
            <label
              >Complete:<input 
              type="checkbox" 
              name="isComplete" 
              <%= todos.isComplete ? "checked" : "" %>/>
            </label>
            <br>
            <button>Update Todo</button> 
        </form>
  </body>
</html>